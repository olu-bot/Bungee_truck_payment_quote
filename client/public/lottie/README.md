Place your Lottie animation JSON files in this folder.

Expected file for signup loading:
- `signup-loading.json`

The signup page automatically loads:
- `/lottie/signup-loading.json`

If the file is missing, the app will show a fallback spinner.
