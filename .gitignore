node_modules/
dist/
.env
.env.local
.DS_Store

# Local gcloud CLI state (credentials, logs — never commit)
.gcloud/

# GCP / Firebase service account JSON downloaded locally (never commit)
bungee-calculator-*.json
*-firebase-adminsdk-*.json
.superpowers/
